{
  "name": "synthetic 0ms budget forces TIMEOUT and yields deny with TIMEOUT reason",
  "force_timeout": true,
  "policy": {
    "version": "fx-timeout",
    "rules": [
      {
        "id": "ma",
        "type": "max_amount",
        "outcome": "deny",
        "parameters": {"threshold": 1}
      }
    ]
  },
  "receipt": {
    "amount_charged": 999,
    "action_type": "api_call",
    "created_at": "2026-05-20T00:00:00Z"
  },
  "expected": {
    "decision": "deny",
    "triggered_rules": [],
    "reasons": ["TIMEOUT"]
  }
}
