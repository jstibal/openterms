{
  "name": "action_type_denylist fires when receipt action_type is on deny list",
  "policy": {
    "version": "fx-atd",
    "rules": [
      {
        "id": "atd",
        "type": "action_type_denylist",
        "outcome": "deny",
        "parameters": {"denied": ["model_training"]}
      }
    ]
  },
  "receipt": {
    "amount_charged": 0,
    "action_type": "model_training",
    "created_at": "2026-05-20T00:00:00Z"
  },
  "expected": {
    "decision": "deny",
    "triggered_rules": ["atd"],
    "reasons": ["ACTION_TYPE_DENYLIST: action_type 'model_training'"]
  }
}
