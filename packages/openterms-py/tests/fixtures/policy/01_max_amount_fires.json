{
  "name": "max_amount fires when amount exceeds threshold",
  "policy": {
    "version": "fx-max_amount",
    "rules": [
      {
        "id": "ma",
        "type": "max_amount",
        "outcome": "deny",
        "parameters": {"threshold": 1000}
      }
    ]
  },
  "receipt": {
    "amount_charged": 2500,
    "action_type": "purchase",
    "created_at": "2026-05-20T12:00:00Z"
  },
  "expected": {
    "decision": "deny",
    "triggered_rules": ["ma"],
    "reasons": ["MAX_AMOUNT: amount_charged 2500"]
  }
}
